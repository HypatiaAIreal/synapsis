// ALTERNATIVA - MovieBlocks con estilos inline (si CSS Modules falla)
import React, { useState, useEffect } from 'react';
import axios from 'axios';
import { ChevronLeft, Lock, Globe, User, Info, Edit3, MessageSquare } from 'lucide-react';
import AnalysisEditor from './AnalysisEditor';

// Objeto con todos los estilos
const styles = {
  container: {
    minHeight: '100vh',
    backgroundColor: '#1a1a2e'
  },
  header: {
    backgroundColor: '#0f0f1f',
    borderBottom: '1px solid rgba(147, 51, 234, 0.2)',
    padding: '16px 24px'
  },
  sidebar: {
    width: '320px',
    backgroundColor: '#0f0f1f',
    borderRight: '1px solid rgba(147, 51, 234, 0.2)',
    overflowY: 'auto' as const,
    flexShrink: 0
  },
  content: {
    flex: 1,
    backgroundColor: '#16162a',
    overflowY: 'auto' as const
  },
  // ... todos los demás estilos convertidos a objetos JS
};

// El resto del componente igual pero usando style={styles.nombre}
// en lugar de className={styles.nombre}